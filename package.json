{
  "name": "social-media-application-root",
  "version": "1.0.0",
  "description": "Root for running social media application client and server",
  "scripts": {
    "server": "cd Social-Media-Application/server && npm start",
    "client": "cd Social-Media-Application/client && npm start",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd Social-Media-Application/client && npm run build",
    "install-all": "npm install && cd Social-Media-Application/server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}